import React from 'react'
import NextHead from 'next/head'
import { string } from 'prop-types'
import Link from 'next/link'

const pages = {

}

const HeadNav = class {
    init(){
        this.props
    }

    render(){
        <Head title={props.title || ''} pages={pages}/>
    }
}
